export enum GameStatus {
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    REJECTED = 'rejected',
    PLAYING = 'playing',
    CANCELLED = 'cancelled',
    LOBBY = 'lobby',
    FINISHED = 'finished',
}
